/**
 * Данный компонент отвечает за логику Яндекс поиска
 * @param items - список над поиском
 * @returns {JSX.Element}
 * @constructor
 */

function SearchComponent({ items }) {
  return(
    <div></div>
  )
}

export default SearchComponent